# ── App Name ──────────────────────────────────────────────
spring.application.name=smart-farming-assistant
server.port=8080

# ── NeonDB PostgreSQL Connection ──────────────────────────
# Replace YOUR_NEON_HOST, YOUR_DB, YOUR_USER, YOUR_PASSWORD
spring.datasource.url=jdbc:postgresql://ep-tiny-cloud-am5et4h2-pooler.c-5.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_HujRzDrC56vo
spring.datasource.driver-class-name=org.postgresql.Driver

# ── JDBC (No JPA – intentional) ───────────────────────────
spring.jdbc.template.query-timeout=30

# ── Connection Pool (HikariCP default) ────────────────────
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=20000

# ── External API Keys (fill these in) ─────────────────────
weather.api.key=2a661541dc20195004b783f900826b82
weather.api.url=https://api.openweathermap.org/data/2.5/weather

openai.api.key=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
openai.api.url=https://api.groq.com/openai/v1/chat/completions

# ── CORS (allow React dev server) ─────────────────────────
spring.web.cors.allowed-origins=http://localhost:5173
# ── Auto-run schema.sql on startup ────────────────────────
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

groq.model=llama-3.1-8b-instant